import styled from 'styled-components';

const StyledWrapper = styled.div`
  .Ratings {
    p {
      margin-bottom: 0;
    }

    ul {
      margin-top: 0;
      padding-left: 2rem;
      list-style: disc;
    }
  }
`;

export default StyledWrapper;
